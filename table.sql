CREATE TABLE personas (
    id SERIAL PRIMARY KEY,
    nombre VARCHAR(255),
    apellido VARCHAR(255),
    Marta VARCHAR(255),
    Ciudad VARCHAR(255),
    Pais VARCHAR(255),
    fecha_nacimiento DATE
);
